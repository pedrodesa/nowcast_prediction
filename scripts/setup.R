source(here::here('install_packages.R'))

# Gera cache de geometria
# ufs <- geobr::read_state(showProgress = FALSE)
# saveRDS(ufs, here::here('data/raw/geometria_ufs.rds'))

log_success('Setup concluído. Projeto pronto para uso.')